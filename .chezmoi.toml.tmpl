{{- $name := promptString "name" -}}
{{- $email := promptString "email" -}}
{{- $location := promptString "location" -}}
{{- $rootDir := promptString "rootDir" -}}
[data]
    name = "{{ $name }}"
    email = "{{ $email }}"
    location = "{{ $location }}"
    rootDir = "{{ $rootDir }}" 
